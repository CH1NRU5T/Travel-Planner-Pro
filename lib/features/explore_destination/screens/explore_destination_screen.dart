import 'package:flutter/material.dart';

class ExploreDestinationScreen extends StatelessWidget {
  const ExploreDestinationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Explore Destination Screen'),
    );
  }
}
